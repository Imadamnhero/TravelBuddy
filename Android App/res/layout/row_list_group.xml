<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <include layout="@layout/row_item_deleting" />

    <RelativeLayout
        android:id="@+id/front"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_row_gradient"
        android:padding="5dp" >

        <FrameLayout
            android:id="@+id/frame_img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginRight="5dp"
            android:background="@drawable/bg_avatar"
            android:padding="3dp" >

            <ImageView
                android:id="@+id/img_group2"
                android:layout_width="@dimen/size_of_avatar"
                android:layout_height="@dimen/size_of_avatar"
                android:scaleType="fitXY"
                android:src="@drawable/default_avatar" />
        </FrameLayout>

        <TextView
            android:id="@+id/textView1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginLeft="20dp"
            android:layout_toRightOf="@+id/frame_img"
            android:text="Jessica"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:textColor="@color/text_gray_color"
            android:textStyle="bold" />
    </RelativeLayout>

</FrameLayout>